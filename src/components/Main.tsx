import "./Main.css";
import Sketch from "react-p5";

const Main = () => {
  // let x = 50;
  // let y = 50;
  // const setup = (p5, parent) => {
  //   p5.createCanvass(700, 500).parent(parent);
  //   p5.background(0);
  // };

  // const draw = (p5) => {
  //   p5.ellipse(x, y, 70, 70);
  //   x++;
  // };
  return (
    <div className="Main">{/* <Sketch setup={setup} draw={draw} /> */}</div>
  );
};

export default Main;
